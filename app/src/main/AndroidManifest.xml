<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.soteria.neurolab">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginCreatePasswordActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SearchCreateDeleteActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".EditPatientDetails" />
        <activity android:name=".ReactionGameActivity" />
        <activity android:name=".TutorialActivity" android:exported="true"/>
        <activity android:name=".MotorSkillsGameActivity" />
        <activity android:name=".VisualMemoryActivity"/>
        <activity android:name=".ViewPatientDetails" />
        <activity android:name=".ViewReportActivity" />
        <activity android:name=".SettingsActivity"/>
        <activity android:name=".LoginSecurityQuestions" />
        <activity android:name=".VisualAttentionGame" />
        <activity android:name=".SelectGameActivity"/>
    </application>

</manifest>